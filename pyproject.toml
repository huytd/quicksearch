[project]
name = "quicksearch-api"
version = "0.1.0"
description = "A simple search API that queries DuckDuckGo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.104.0",
    "httpx>=0.25.0",
    "beautifulsoup4>=4.12.0",
    "uvicorn>=0.24.0"
]
