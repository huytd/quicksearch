# Quick Search API

A simple web search service that proxy the queries to DuckDuckGo and return results as JSON.

Endpoints:

- `/search?q=<query>`: Search with query and get the results
- `/read?url=<url>`: Read the URL content as plain text


